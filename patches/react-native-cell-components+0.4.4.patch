diff --git a/node_modules/react-native-cell-components/lib/theme.js b/node_modules/react-native-cell-components/lib/theme.js
index 94c96a8..c1f1007 100644
--- a/node_modules/react-native-cell-components/lib/theme.js
+++ b/node_modules/react-native-cell-components/lib/theme.js
@@ -26,7 +26,7 @@ const colors = {
 };
 
 // https://github.com/ptelad/react-native-iphone-x-helper/blob/master/index.js
-function isIphoneX() {
+function isIphoneXHelper() {
     let dimen = Dimensions.get('window');
     return (
         Platform.OS === 'ios' &&
@@ -43,13 +43,13 @@ const value = (ios = null, android = null) => {
   });
 };
 
-const isIphoneX = isIphoneX();
+const isIphoneX = isIphoneXHelper();
 const borderWidth = StyleSheet.hairlineWidth;
 const radius = value(5, 2);
 const padding = value(10, 16);
 const margin = value(10, 8);
-const bottomOffset = isIphoneX ? 20 : 0;
-const topOffset = isIphoneX ? 60 : 20;
+const bottomOffset = isIphoneXHelper ? 20 : 0;
+const topOffset = isIphoneXHelper ? 60 : 20;
 
 const borders = {};
 const borderRadius = {};
@@ -99,7 +99,7 @@ export default theme = {
   },
   isAndroid: value(false, true),
   isIOS: value(true, false),
-  isIphoneX,
+  isIphoneX: isIphoneXHelper,
   bottomOffset,
   topOffset,
   borderWidth,
